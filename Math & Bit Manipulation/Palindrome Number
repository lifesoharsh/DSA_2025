class Solution {
public:
    bool isPalindrome(int x) {
        string num = to_string(x);
        int i = 0, j = num.length()-1;
        while(i < j){
            if(num[i++] != num[j--]) return false;
        }
        return true;
    }
};
