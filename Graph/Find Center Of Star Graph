class Solution {
public:
    int findCenter(vector<vector<int>>& edges) {
        int u = edges[0][0];
        int v = edges[0][1];
        int w = edges[1][0];
        int x = edges[1][1];

        if(u == w || u == x) return u;
        return v;
    }
};
