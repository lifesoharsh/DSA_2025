bool lsearch(vector<int>& arr, int k) {
        bool found=false;
        for(int i=0;i<arr.size();i++){
            if(arr[i]==k){
                found=true;
                break;
            }
        }
        return found;
    }
